spring:
  application:
    name: StudentManagementSystemTeacher
  datasource:
    url: ${url}
    username: ${username}
    password: ${password}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    generate-ddl: true
server:
  servlet:
    context-path: /api
  port: 8084